# Historical Engineering Notes

Files in this directory preserve investigations and implementation plans from
earlier GhostGUI revisions. They may reference removed paths, old interface
labels, or behavior that has since changed.

Use the current [documentation index](../README.md) for supported behavior.
Treat these notes as historical context only, and verify every claim against the
current source before reusing it.
